# ARI Database Schema (Reference Only)

These SQL files are historical reference copies of the ARI/fancyrobot schema.

**The source of truth for all migrations is `core/supabase/migrations/`.**

Do NOT apply these files directly. They are kept for documentation purposes.
